# 实时 AI 电脑操作辅助助手开发计划

## 0. 项目目标与范围

### 总体目标

构建一个运行在本地的 AI 电脑操作辅助助手，核心能力包括：

1. 持续采集屏幕内容，并以低延迟方式生成屏幕理解结果。
2. 使用 `Qwen3.5-2B-GGUF` 通过 `llama.cpp` 运行边缘模型，完成视觉/文本理解、任务规划和操作建议。
3. 建立独立的 UI 识别框架，用于快速发现并定位按钮、输入框、菜单、列表、窗口、图标等屏幕元素，为大模型提供结构化候选目标和精确坐标。
4. 在用户授权和安全约束下执行鼠标点击、移动、滚轮、键盘输入等电脑操作。
5. 支持“实时解读 + 用户确认 + 执行 + 结果验证”的闭环，并能够在操作失败时停止、回退或请求人工介入。

### 第一阶段明确不做的事项

- 不默认允许模型无确认地执行高风险操作，如删除文件、转账、发消息、提交订单、修改系统设置等。
- 不把模型输出的自然语言直接转换为系统输入；所有操作必须经过结构化协议、权限检查和坐标/目标校验。
- 不在第一版追求覆盖所有应用和所有复杂视觉场景，优先支持浏览器、文本编辑器、文件管理器等典型桌面应用。

## 1. 建议的总体架构

```text
屏幕采集层
    ├─ 全屏/窗口/区域截图
    ├─ 差分检测、帧率控制、缩放和裁剪
    └─ 屏幕变化事件
          ↓
实时感知编排器
    ├─ UI 识别框架（快速、频繁）
    ├─ OCR / 无障碍树 / 应用原生语义（可选增强）
    ├─ 屏幕状态缓存与元素索引
    └─ 触发 Qwen 视觉理解的策略
          ↓
模型推理层（llama.cpp）
    ├─ Qwen3.5-2B-GGUF 加载与推理
    ├─ prompt/template 管理
    ├─ 多模态输入适配（按实际模型能力确认）
    └─ 结构化 JSON 输出约束
          ↓
任务与操作层
    ├─ 意图解析 / 任务分解
    ├─ 操作计划和风险分级
    ├─ 用户确认与权限控制
    ├─ 鼠标键盘执行器
    └─ 执行后屏幕状态验证
          ↓
UI / API / 日志
    ├─ 实时屏幕解读
    ├─ 目标高亮与操作预览
    ├─ 暂停、继续、撤销和紧急停止
    └─ 性能、错误和审计信息
```

### 模块边界

- `capture`：只负责可靠、低延迟地取得屏幕帧，不承担语义判断。
- `ui_vision`：把屏幕转换为 `UIElement[]`，提供类型、文本、边界框、置信度、可交互性和来源。
- `inference`：封装 llama.cpp，屏蔽模型加载、上下文、批处理、GPU offload 和输出解析细节。
- `agent`：管理用户任务、屏幕状态、规划、确认和状态机，不直接调用底层系统 API。
- `actuator`：执行经过校验的结构化动作，并返回结果。
- `safety`：统一处理敏感应用、敏感操作、用户确认、限速、超时、紧急停止和审计。
- `frontend`：展示实时解读、识别框、当前计划、确认提示和错误状态。

## 2. 阶段计划

## 阶段 A：需求、运行环境与技术可行性验证

- [x] 确认目标操作系统和发布形态（Windows 优先，不需要 macOS/Linux）。
- [x] 确认 Qwen3.5-2B-GGUF 的具体模型文件、量化版本、上下文长度和实际多模态输入格式。
- [x] 确认模型是否能直接接收图像；若当前 GGUF 仅支持文本，需要明确视觉编码器、mmproj 或额外视觉模型的集成方式。
- [x] 确定开发语言和进程模型：建议将采集、识别、推理、执行拆分为可测试的模块，必要时将 llama.cpp 放在独立推理进程中。
- [x] 建立硬件基线：CPU、GPU、显存、内存、屏幕分辨率、多屏数量和操作系统版本。
- [x] 记录基线指标：模型加载时间、单次推理延迟、峰值内存/显存、截图耗时和空闲 CPU 占用。
- [x] 用 20~30 个代表场景验证模型能否完成：读取页面、识别按钮、判断输入框、描述当前状态、输出目标坐标/元素 ID。
- [x] 形成 `docs/feasibility.md`，记录模型限制、依赖版本、已知失败案例和最终技术选型。

**阶段验收：** 可以在本机加载模型，完成一张屏幕截图的稳定推理，并得到可解析的结构化结果；如果模型本身不具备视觉能力，必须在此阶段确定补充视觉链路，而不是在后续阶段默认模型可以看图。

**阶段 A 完成记录（2026-08-11）：**
- 后端：`D:\llama\llama-server` + oneAPI SYCL，`SYCL0` Intel Arc A770，`-ngl 99`（**非 CPU**）
- 模型：`Qwen3.5-2B-UD-Q4_K_XL.gguf` + `mmproj-F16.gguf`（原生多模态，可看图）
- 验收：截图视觉推理 JSON 通过；场景 **27/30**；详见 `docs/feasibility.md` 与 `benchmarks/phase_a/results/`

## 阶段 B：项目骨架与基础工程能力

- [x] 创建项目目录：`capture/`、`ui_vision/`、`inference/`、`agent/`、`actuator/`、`safety/`、`frontend/`、`tests/`、`benchmarks/`、`docs/`（另含 `core/` 共享层与 `config/`）。
- [x] 配置统一的构建、格式化、静态检查、单元测试和本地运行命令（`pyproject.toml`：setuptools / ruff / pytest / mypy；`docs/setup.md`；`scripts/dev_check.bat`）。
- [x] 定义统一的数据模型和版本号，避免模块之间直接传递未约束的自然语言（`core/models.py`，`PROTOCOL_VERSION=1.0.0`；`docs/protocol.md`）。
- [x] 建立配置文件：模型路径、上下文大小、线程数、GPU layers、截图帧率、识别阈值、确认策略和日志级别（`config/default.yaml` + `core/config.py` + 环境变量覆盖）。
- [x] 建立结构化日志与 trace ID，区分屏幕帧、识别结果、模型请求、计划、动作和验证结果（`core/logging.py` + `core/pipeline.py` 事件链）。
- [x] 设计错误分类：模型加载失败、截图失败、识别超时、输出解析失败、权限不足、目标失效、操作失败和验证失败（`core/errors.py` → `ErrorCode`）。
- [x] 增加全局取消机制和优雅退出机制（`core/cancel.py`；CLI 安装 SIGINT/SIGTERM）。

**阶段验收：** 新环境可以按文档启动；各模块可以用 mock 数据独立运行；日志能关联一次从截图到动作验证的完整链路。

**阶段 B 完成记录（2026-08-11）：**
- 目录与包：`capture` / `ui_vision` / `inference` / `agent` / `actuator` / `safety` / `frontend` / `core` / `tests` / `config`
- 默认后端均为 **mock**；`actuator.dry_run=true`（不注入真实键鼠）；`inference.http_client` 为阶段 E 预留
- 验收命令：`pip install -e ".[dev]"` → `pytest` → `python -m frontend.cli demo --goal "点击搜索按钮"`
- 文档：`README.md`、`docs/setup.md`、`docs/protocol.md`、`docs/architecture_b.md`

## 阶段 C：屏幕采集与实时帧管线

- [ ] 实现全屏、指定窗口和指定区域三种采集模式。
- [ ] 支持多显示器、不同 DPI/缩放比例、坐标原点和屏幕旋转等坐标系统处理。
- [ ] 对截图进行缩放、色彩转换、压缩和 ROI 裁剪，减少传给识别器和模型的数据量。
- [ ] 实现帧变化检测：只有屏幕发生明显变化、用户提出新问题或当前动作需要验证时才触发后续处理。
- [ ] 设计有界队列和丢帧策略，保证推理变慢时不会无限堆积旧帧。
- [ ] 区分“预览帧”“识别帧”“模型帧”“验证帧”，分别配置频率和质量。
- [ ] 记录截图时间戳、屏幕尺寸、DPI、窗口信息和 frame ID。
- [ ] 处理敏感区域遮罩，支持密码框、隐私窗口和用户手动屏蔽区域。

**建议初始目标：** UI 识别在稳定场景下约 5~15 FPS；大模型视觉理解按事件触发，不强制每帧运行；端到端首个可用结果目标为 1~3 秒，再根据硬件优化。

**阶段验收：** 在单屏和多屏、不同缩放比例下，截图坐标与实际鼠标坐标一致；高频屏幕变化不会导致内存持续增长或队列失控。

## 阶段 D：UI 识别框架

### D1. 统一元素协议

- [ ] 定义 `UIElement`：`id`、`type`、`text`、`role`、`bbox`、`center`、`confidence`、`visible`、`enabled`、`clickable`、`editable`、`source`、`timestamp`。
- [ ] 定义元素类型：`window`、`dialog`、`button`、`input`、`checkbox`、`radio`、`tab`、`menu`、`menu_item`、`link`、`list`、`table`、`icon`、`text`、`image` 等。
- [ ] 所有坐标统一使用屏幕物理像素，同时保留逻辑坐标和 DPI 信息，避免缩放导致误点。
- [ ] 为每个识别结果生成稳定的短期 `element_id`，并通过 frame ID / hash 判断元素是否已经失效。

### D2. 多来源识别管线

- [ ] 优先接入操作系统无障碍/UI Automation 树，获取准确角色、文本、可操作性和原生边界框。
- [ ] 接入 OCR，用于识别 UI Automation 无法覆盖的文字、画布、远程桌面和自绘控件。
- [ ] 接入轻量级视觉检测模型或规则检测，用于定位图标、按钮、输入框、复选框、窗口边界和常见控件。
- [ ] 对多个来源做去重、融合和置信度校准。
- [ ] 对元素做层级化组织：窗口 → 容器 → 控件 → 文本/图标。
- [ ] 支持 ROI 识别，只重新分析变化区域。
- [ ] 设计识别器插件接口，便于未来替换 OCR、检测模型或接入特定应用适配器。

### D3. 为 Qwen 提供紧凑上下文

- [ ] 将识别结果序列化为紧凑 JSON/文本，包含元素 ID、类型、文本、坐标、状态和置信度。
- [ ] 根据用户问题和当前任务筛选相关元素，避免把整个屏幕的冗余信息塞入上下文。
- [ ] 对屏幕截图绘制编号框，模型输出使用 `element_id`，禁止优先依赖裸坐标。
- [ ] 对低置信度或冲突元素标记为需要模型复核/用户确认。
- [ ] 建立 UI 识别评测集，覆盖正常控件、暗色主题、小字体、遮挡、滚动、弹窗、远程桌面和自绘界面。

**阶段验收：** 对评测集中的常见交互元素，能够返回准确的元素类型、文本和边界框；模型可以用元素 ID 指代目标，而不是依赖不稳定的自然语言描述或手写坐标。

## 阶段 E：llama.cpp 推理层与 Qwen 适配

- [ ] 固定并记录 llama.cpp 版本、编译选项和平台依赖。
- [ ] 实现模型生命周期：加载、预热、推理、取消、释放和异常恢复。
- [ ] 调整 `n_ctx`、线程数、批大小、GPU offload layers、KV cache 和 flash attention 等参数，并建立基准记录。
- [ ] 实现文本、结构化 UI 摘要和图像输入的统一请求接口；若视觉输入需要额外投影文件或服务，封装为明确的适配器。
- [ ] 为 Qwen 配置正确的 chat template、system prompt、停止词和最大输出长度。
- [ ] 使用 JSON schema 或 grammar 约束输出，定义 `ScreenObservation`、`ActionPlan`、`Action`、`Verification` 等协议。
- [ ] 实现流式输出，但只有在 JSON 完整且通过校验后才允许进入操作层。
- [ ] 对模型输出进行 schema 校验、坐标范围校验、元素存在性校验和指令白名单校验。
- [ ] 增加超时、取消、重试和降级策略：模型忙时返回最近可信状态或请求用户确认。
- [ ] 建立 prompt 版本管理和回归样例，避免修改提示词后能力悄然下降。

**阶段验收：** 输入截图/结构化 UI 状态和用户任务后，模型能稳定输出合法的观察结果或动作计划；所有非法、越权和不完整输出都会被拒绝，不会直接触发系统输入。

## 阶段 F：任务代理与操作状态机

- [ ] 定义任务状态：`idle` → `observing` → `planning` → `awaiting_confirmation` → `executing` → `verifying` → `completed/failed/paused`。
- [ ] 支持用户自然语言任务，例如“打开浏览器并搜索……”“点击设置”“把这段文字输入到当前输入框”。
- [ ] 将任务拆分为短步骤，每一步都绑定目标元素、前置条件、预期变化、风险等级和超时。
- [ ] 优先使用 `element_id` 执行动作；只有在元素识别不可用时，才允许使用经过二次确认的坐标。
- [ ] 定义基础动作：鼠标移动、单击、双击、右键、拖拽、滚轮、键盘按键、文本输入、快捷键、等待和重新识别。
- [ ] 实现动作前重新截图/重新定位，防止页面变化后的旧坐标误操作。
- [ ] 实现动作后验证：检查目标元素状态、页面文本变化、窗口变化或截图差异。
- [ ] 支持失败恢复：重新识别、回到上一步、跳过可选步骤、暂停并请求用户处理。
- [ ] 增加用户可见的“即将执行什么、目标在哪里、预计影响是什么”的预览。

**阶段验收：** 能完成至少三个低风险端到端任务，并且每一步都有可追踪的目标、执行结果和验证结果；页面变化或目标消失时会暂停，而不是盲目点击原坐标。

## 阶段 G：安全、权限与隐私

- [ ] 建立风险分级：只读观察、低风险交互、中风险数据修改、高风险外部提交/不可逆操作。
- [ ] 默认只读；低风险操作可由用户配置自动执行；中高风险操作必须逐步或最终确认。
- [ ] 为敏感动作建立硬编码拦截和确认策略，包括删除、支付、转账、发送、发布、安装、权限变更和密码输入。
- [ ] 支持全局暂停热键、紧急停止按钮和失去焦点自动暂停。
- [ ] 限制动作频率、连续动作数量、单次任务最大时长和最大鼠标移动范围。
- [ ] 对目标应用、窗口标题、域名和敏感区域设置允许/禁止列表。
- [ ] 屏幕图像、OCR 文本、模型上下文和操作日志默认仅本地保存，并提供关闭持久化和清理功能。
- [ ] 密码框、银行卡号、身份证号等内容默认遮罩，不发送给模型、不写入日志。
- [ ] 记录不可抵赖的本地审计信息：用户任务、模型版本、识别结果摘要、确认记录、动作和结果。
- [ ] 完成威胁建模，覆盖 prompt injection、恶意网页指令、模型幻觉、坐标欺骗、剪贴板泄漏和越权操作。

**阶段验收：** 通过安全测试集中的高风险场景；即使模型输出“立即执行”，也不能绕过权限、确认和动作白名单。

## 阶段 H：用户界面与可观测性

- [ ] 实现常驻入口或主窗口：开始/暂停、任务输入、当前状态和紧急停止。
- [ ] 显示实时屏幕解读、识别到的 UI 元素和目标高亮框。
- [ ] 显示当前计划、即将执行的动作、风险级别和确认按钮。
- [ ] 支持用户修正目标：“不是这个按钮”“点击这里”“忽略该区域”，并将修正作为当前任务上下文。
- [ ] 展示模型延迟、识别延迟、队列长度、CPU/GPU/内存占用和最近错误。
- [ ] 增加开发者诊断视图：原始截图、ROI、UI 树、OCR、融合结果、prompt、结构化输出和验证结果。
- [ ] 为隐私设计可见状态提示，让用户明确知道何时正在采集屏幕、何时正在推理和何时即将执行操作。

## 阶段 I：测试、评测与性能优化

### 测试

- [ ] 为坐标转换、多屏/DPI、帧队列、元素去重、schema 校验、风险分级和状态机编写单元测试。
- [ ] 为采集 → 识别 → 推理 → 计划 → 执行 → 验证编写集成测试。
- [ ] 使用可回放的屏幕帧和 UI 树构建离线回归测试，避免测试依赖真实桌面状态。
- [ ] 使用受控的沙箱应用测试点击、输入、弹窗、滚动、拖拽和失败恢复。
- [ ] 测试模型幻觉、空输出、乱码、超长输出、恶意指令和格式错误。
- [ ] 测试高 DPI、多显示器、窗口切换、锁屏/休眠、远程桌面和权限变化。

### 评测指标

- [ ] 屏幕采集延迟与稳定帧率。
- [ ] UI 元素检测 precision/recall、文字识别准确率、bbox IoU、元素 ID 选择准确率。
- [ ] 首次理解延迟、计划生成延迟、端到端动作延迟。
- [ ] 任务完成率、错误点击率、需要人工接管率和误操作率。
- [ ] 模型加载时间、峰值内存/显存、持续运行 CPU 占用和功耗。
- [ ] 在不同量化版本、分辨率、ROI 策略和硬件后端下做对比。

### 优化方向

- [ ] 采用事件触发、帧差、ROI 和缓存，减少无意义的大模型调用。
- [ ] 让 UI 识别器承担高频定位，让 Qwen 主要承担语义判断、规划和歧义消解。
- [ ] 压缩 UI 上下文，避免重复传递完整屏幕和历史信息。
- [ ] 对简单问题使用规则/OCR/UI 树直接回答，复杂问题再升级到模型。
- [ ] 根据硬件选择量化版本和 CPU/GPU 混合推理策略。
- [ ] 评估模型并发、批处理和多级模型方案，但不要在第一版引入未经验证的复杂调度。

## 阶段 J：打包、发布与长期维护

- [ ] 确定模型分发方式、许可证说明、模型文件校验和首次下载/导入流程。
- [ ] 提供模型路径配置、缺失依赖提示、GPU 不可用时的 CPU fallback 和诊断报告。
- [ ] 对 llama.cpp、OCR、UI Automation 和系统输入权限建立版本兼容矩阵。
- [ ] 处理崩溃恢复、日志轮转、模型进程重启和应用升级。
- [ ] 编写用户文档：安装、权限、隐私、确认策略、支持的应用、常见故障和卸载。
- [ ] 编写开发文档：架构、协议、模型适配、识别器插件、测试和 benchmark 流程。
- [ ] 建立回归评测门禁：模型、prompt、识别器或系统 API 变更后必须重新跑关键场景。
- [ ] 规划后续能力：应用专用适配器、更强视觉模型、长期任务记忆、语音输入和远程控制（需单独安全评审）。

## 3. 第一版 MVP 建议范围

第一版优先完成以下闭环：

- [ ] Windows 单屏/双屏截图采集和 DPI 正确坐标转换。
- [ ] 基于 UI Automation + OCR 的按钮、输入框、文本和窗口识别。
- [ ] llama.cpp 加载 Qwen3.5-2B-GGUF，并输出结构化屏幕摘要和低风险动作计划。
- [ ] 支持“读取当前页面”“定位某个按钮”“点击指定按钮”“向指定输入框输入文本”四类任务。
- [ ] 所有点击和输入默认需要用户确认，并在执行前显示高亮框和动作预览。
- [ ] 执行后重新识别并验证结果，失败时自动暂停。
- [ ] 提供暂停/继续/紧急停止、敏感区域遮罩和本地日志。
- [ ] 完成一套离线屏幕回放测试和基础性能报告。

## 4. 推荐的结构化协议

### UI 元素

```json
{
  "element_id": "btn_search_01",
  "type": "button",
  "role": "button",
  "text": "搜索",
  "bbox": { "x": 1200, "y": 80, "width": 96, "height": 36 },
  "confidence": 0.97,
  "clickable": true,
  "source": ["uia", "ocr"],
  "frame_id": "frame-000123"
}
```

### 动作计划

```json
{
  "goal": "点击搜索按钮",
  "steps": [
    {
      "action": "click",
      "target_element_id": "btn_search_01",
      "risk": "low",
      "requires_confirmation": true,
      "preconditions": ["element.visible == true", "element.clickable == true"],
      "expected_change": "搜索区域或结果列表发生变化"
    }
  ],
  "stop_if": ["target_missing", "window_changed", "confidence_below_threshold"]
}
```

## 5. 近期执行顺序

1. 完成阶段 A，特别是确认 Qwen3.5-2B-GGUF 的真实视觉输入能力和运行参数。
2. 建立阶段 B 的项目骨架、协议、日志和配置。
3. 先完成阶段 C 的截图/坐标管线，再做任何自动点击，确保坐标正确。
4. 实现阶段 D 的 UI 元素统一协议和第一批识别来源。
5. 完成阶段 E 的 llama.cpp 封装及结构化输出校验。
6. 用只读屏幕问答验证模型，再接入阶段 F 的低风险点击/输入闭环。
7. 在扩大任务范围前完成阶段 G 的安全策略、阶段 I 的回放测试和失败恢复。

## 6. 关键风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| Qwen3.5-2B-GGUF 实际不支持直接图像输入 | 核心视觉链路无法工作 | 阶段 A 强制验证；接入匹配的视觉编码器/适配器或独立 UI 视觉模型 |
| 2B 模型对复杂页面和长任务理解不足 | 规划错误、任务完成率低 | 用 UI 识别结果压缩上下文；限制步骤长度；每步验证；必要时采用模型分级 |
| DPI/多屏坐标错误 | 误点击 | 统一物理像素坐标；建立多屏自动化测试；执行前高亮预览 |
| 屏幕变化导致识别结果过期 | 点击错误目标 | frame ID、元素 hash、执行前重新定位和失效即暂停 |
| 全屏持续推理带来高延迟和高功耗 | 体验不可用 | 帧差、ROI、事件触发、缓存、轻量识别高频运行 |
| 网页或文档中的恶意提示诱导模型操作 | 越权或危险行为 | 将屏幕文字视为不可信数据；系统策略优先；动作白名单和用户确认 |
| 模型输出不可解析或幻觉坐标 | 操作失败 | JSON grammar/schema、元素 ID、范围校验、置信度门槛、失败暂停 |
| 敏感屏幕内容进入日志或上下文 | 隐私泄露 | 遮罩、最小化上下文、本地处理、日志脱敏和可清理持久化 |

## 7. 完成定义（Definition of Done）

- [ ] 在目标硬件上可重复启动并加载指定模型。
- [ ] 屏幕采集、UI 识别和模型推理可实时运行，资源占用和延迟有测量数据。
- [ ] 模型输出始终经过结构化校验，非法输出不会到达操作执行器。
- [ ] 至少三个低风险任务能够完成，并有动作后验证和失败暂停。
- [ ] 多屏/DPI、窗口变化、目标消失和模型超时均有明确处理。
- [ ] 高风险操作有强制确认或拦截，具备全局紧急停止。
- [ ] 敏感内容默认不进入日志，用户能看到采集、推理和执行状态。
- [ ] 有自动化测试、离线回放样例、性能报告、安装文档和已知限制说明。

